# Chill Smoke Archive - Firebase Direct Upload版

GitHub Pagesで公開しながら、画像はFirebase Storageへ直接アップロードし、商品データはCloud Firestoreへ保存する版です。

## ファイル構成

```text
index.html
style.css
app.js
firebase-config.js
firebase-rules.txt
```

## できること

- 開封前画像アップロード
- 開封後画像アップロード
- 商品名登録
- 恒常商品 / 限定商品
- ベータ / 本リリースv1 / 本リリースv2
- 商品説明
- 販売期間
- メモ
- 一覧表示
- 検索
- フィルター
- 詳細表示
- 編集
- 削除

## セットアップ手順

### 1. Firebaseプロジェクトを作る

Firebase Consoleで新規プロジェクトを作成します。

### 2. Webアプリを追加

Firebase Consoleのプロジェクト設定からWebアプリを追加します。

表示される `firebaseConfig` をコピーして、`firebase-config.js` に貼り付けます。

```js
FIREBASE_CONFIG: {
  apiKey: "...",
  authDomain: "...",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "..."
}
```

### 3. Authenticationを有効化

Authentication → Sign-in method から `Anonymous` を有効化します。

### 4. Firestore Databaseを作成

Firestore Databaseを作成します。

コレクション名は初期設定だと `products` です。

### 5. Storageを有効化

Cloud Storage for Firebaseを有効化します。

画像は初期設定だと `chill-smoke-archive/` 以下に保存されます。

### 6. ルールを設定

`firebase-rules.txt` の内容をFirebase Console側に貼り付けます。

### 7. ADMIN_PINを変更

`firebase-config.js` の `ADMIN_PIN` を変更してください。

```js
ADMIN_PIN: "好きなPIN"
```

これは仲間内向けの簡易ロックです。
GitHub Pages上のフロントエンドに入るため、本格的なセキュリティではありません。

### 8. GitHub Pagesへアップロード

このフォルダの中身をGitHubリポジトリにアップロードします。

`Settings` → `Pages` → Branchを選択して公開します。

## 注意点

- GitHub Pages単体では画像保存はできません。
- Firebaseの無料枠を超えると課金や制限が発生する可能性があります。
- 短期・仲間内運用ならこの構成で十分使いやすいです。
- 本格運用するなら、Googleログイン制限や管理者だけ書き込みできるルールに変更してください。
